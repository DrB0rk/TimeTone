DATABASE_PATH=./data/timekeep.db
ADMIN_PASSWORD=change-this-password
ADMIN_SECRET=generate-a-long-random-secret
DEV_DEVICE_TOKEN=replace-with-a-random-device-token
# Set true when the app is served through HTTPS. Keep false for plain LAN HTTP.
COOKIE_SECURE=false
